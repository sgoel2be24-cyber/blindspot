# Local data directory

Place accepted IEEE-CIS files in `data/raw/ieee-cis/` as described in `../DATA_PROVENANCE.md`.

Nothing under `raw/` or `processed/` is committed. The automated test suite uses generated synthetic rows instead.
